# session-2026-09-20-macos (8줄 요약)

1. T-355 메뉴바 전환: `MenuBarExtra(.menu)` → 수동 `NSStatusItem`+`NSPopover`(Etchost `StatusItemController` 패턴 이식). T-356 설정 탭 깜빡임: 일반·채팅·도구 탭의 바깥 `ScrollView`(스크롤 중첩) 제거→`Form(.grouped)`으로 5탭 통일. T-357 좌우 여백: 공용 `View.dsSettingsForm()`(grouped+세로 16pt)로 5곳 통일. T-358 좌우 여백 근본 수정: 원인은 패딩이 아니라 창 크기(기본 900 vs 내용 560 → grouped 폼 가운데 정렬) → 내용 폭 600 고정+`Settings` 씬 `.windowResizability(.contentSize)`+사방 균일 `.padding(DSSpace.l)`+저장 프레임(900) 키 1회 삭제. AX 실측 600×508, 좌 16/우 16·카드 36/36 대칭. 창 제목은 SwiftUI가 선택 탭 이름으로 덮어써 `SettingsWindowTitleSetter`(KVO 감시→"LiteRT-LM Studio 설정" 재고정)로 고정, 5탭 전환 모두 유지. T-359 설정 탭 재구성: `웹 도구 사용`→도구>웹, `대화 기록 전송`→채팅>동작, `첫터치 프리필`→일반>시스템, `벤치마크 보관`→일반>고급, 채팅 탭에 `표시`/`동작` 섹션+글자 크기·세션 정렬 신규, 도구 탭 작업폴더 `파일·셸` 섹션화, 설정의 MTP·적용 제거→인스펙터 일원화(설정 창 열 때 인스펙터 초안 revert되던 회귀 해소). 빌드 OK·311/311·lint 신규 0, 눈확인 완료. T-360 경고 정리(동작 불변): swiftlint 11→0 — `ToolCallFormat`(ToolEvents 447→333)·후속질문 뷰 3종(FollowUpSuggest 403→350)·벤치마크 확장(NativeEngine 398→367) 새 파일 분리, `streamEvents`→`StreamInput`+`pump`/`pumpWithRetry`, `request`→`performRequest(_ PendingRequest)`(파라미터 6→1), `runServerTurns`→`streamServerTurn`, `firstObjectEnd`→`JSONObjectScanner`, `getCachedBackends`→`CachedBackends`, 수명주기 5함수 확장 이동. 컴파일러: `ALWAYS_SEARCH_USER_PATHS=NO`(헤더맵), `FollowGate.find` superview→`MainActor.assumeIsolated`, `DaemonManager.port`→`nonisolated`. 잔여 경고 2건은 벤더 `Core/LiteRTLM` Sendable로 T-010 정책상 미수정.
2. 신규 파일 2개: `StatusItemController.swift`(상태 아이템·팝오버·툴팁), `MenuBarPopover.swift`(상태 카드+빠른 동작+최근 채팅방 3). `project.pbxproj`에 `4000…154/155`·`5000…154/155` 등록.
3. 팝오버 구성: 상태 카드(상태·:9379·가동시간·모델) → 서버 시작/중지 → 새 채팅 → 최근 채팅방 3(클릭 전환) → 메인 창 → 모델 관리 → 설정/종료. `NSHostingController.sizingOptions=[.preferredContentSize]`로 자동 높이.
4. 동기화 수정: 팝오버/라벨이 `AppServices`만 구독하면 중첩 `daemon`·`chat`·`nativeEngine` 변경이 전파되지 않음 → 각각 `@EnvironmentObject`로 직접 구독. 점·문구·툴팁은 **현재 전송 경로(서버/앱 내 엔진)를 구분**(`MenuBarRouteStatus`·`MenuStatus.dotKey(route:…)`, 테스트 +2, 토글 버튼 `MenuBarActionText`(서버/앱 내 엔진 준비·중지) 추가).
5. 아이콘: 기존 `MenuBarLabel`(칩 템플릿+상태 점)을 `PassthroughHostingView`(hitTest=nil)로 `NSStatusBarButton`에 얹어 외형 유지. `popover.behavior=.transient`, `preferredEdge:.maxY`, `makeKey`+`activate`.
6. 팝오버는 씬 밖 → `@Environment(\.openWindow)` 불가. `.openMainWindow` 노티 신설 + 메인 `Window` 씬 `.onReceive` + AppDelegate AppKit 폴백(`makeKeyAndOrderFront`). 모델 관리·새 채팅은 메인 창 연 뒤 지연 전달(`deliver`).
7. 공유 접근: `AppServices.shared`(약참조, init 등록). `AppDelegate`가 `statusController` 보유(창 닫아도 메뉴바 유지). 순수 헬퍼 `MenuBarStatusText.line/tooltip/uptimeText`·`MenuBarRecents.recent`(테스트 +2), 구 `MenuBarView`(텍스트 메뉴) 삭제.
8. 규칙: main 직접 push 금지(빈 저장소 초기 push만 예외 승인), 파괴적 변경 확인, 한국어. 완료: T-355~T-359 커밋·push(`d9b35ae`·`6cbe091`), `README.md`/`README.ko.md`/`LICENSE`(Apache-2.0) 작성 후 PR #1 생성→사용자 머지(`9c55e04`·`f126f0d`). T-360(경고 정리) 커밋 `91b5f2b`→PR #2 사용자 머지(`5e7aea9`), main 동기화 후 원격 브랜치 삭제. 다음: `feat/i18n`(T-361 다국어 인프라) 커밋·push·PR — 벤더 Sendable 경고 2건은 정책상 유지.
9. T-361 다국어 인프라: 조사 결과 로컬라이징 전무(`.xcstrings`/`NSLocalizedString` 0건) — 앱 한국어 리터럴 약 1,740(고유 1,259), UI 인자 약 401, 로거 약 302, 테스트 한국어 단언 225줄. `developmentRegion=ko`·`knownRegions=(en,ko)`는 이미 설정. 신규 `Core/Localization.swift`(AppLanguage·`L10nKey`·`LanguageStore`(NSLock, `.lproj` 번들 조회, 미존재 시 키 원문)·전역 `L`/`T`/`LK`·`LanguageManager`(`appLanguage` 기본 `.system`, `AppleLanguages` 기록)·`View.languageAware`(locale+`.id` 강제 리빌드)), `Core/LocalizationKeys.swift`(시맨틱 키, swiftlint nesting 때문에 `L10n.Settings.generalX` 1단계), `Localizable.xcstrings`(ko 소스·9키 ko/en). 설정>일반>외관 언어 세그먼트+즉시 적용 안내. pbxproj 4파일 등록+`LOCALIZATION_PREFERS_STRING_CATALOGS=YES`. 커버리지 테스트 `LiteRTLMStudioLocalizationTests`(+4), 315/315·build OK·lint 0.
10. 주의: `L10n` 상수는 전부 명시(`L(L10n.Common.cancel)`) — `L(.x)`는 타입 추론 실패. `SWIFT_EMIT_LOC_STRINGS=NO` 유지(한국어 리터럴 대량 잔존 시 자동 추출 방지). `LOCALIZATION_PREFERS_STRING_CATALOGS` 변경 시 전 소스 재컴파일되어 잠복 경고가 드러남(MCPClient·ModelCatalog Sendable 캡처, ModelStore·SystemMonitor MainActor static, BenchmarkHistory 불필요 await, 벤더 Highlightr deprecation — 전부 기존 코드).
11. T-362 키 체계+순수 로직 전환: `L10n` 네임스페이스 15종 추가(외관·권한·엔진·기록·상태·벤치·모델카탈로그·모델단계·세션·사이드바·인스펙터·스킬·시간·도구상태·메뉴바), `ToolCatalog`(키 파생+카테고리 ASCII rawValue)·`ModelAlias.modalities`·`ModelCatalog`·`BenchmarkHistory`(stage/estimate/elapsed/status/retention/summary/analysis, `allModelsToken`으로 표시-저장 분리)·`ModelStore`·`ChatStore`·`SkillsStore`·`UnifiedStatus`·`MessageBubbles.chatRelativeTime`·`MenuBar*Text`를 키 기반으로 전환. 전체 기록 센티넬을 토큰으로 교체. 키 전수 인덱스는 `Core/LocalizationKeyIndex.swift` 분리(file_length·type_body_length 회피). 카탈로그 147키 ko/en. 테스트는 `LiteRTLMStudioTestCase`(ko 고정)로 결정화 + 로컬라이징 테스트 7종 보강(영어 전환·포맷 인자·전 키 해석). 318/318·build OK·lint 0.
12. T-363 화면 치환(영역별 12커밋): 키 파일을 `LocalizationKeys{Screens,Models,Views,Core}.swift`로 분할하고, `LocalizationKeyIndex`를 3키/행 압축 포맷으로 재생성하는 스크립트로 키 레지스트리(현재 774키)·카탈로그(814키)를 커밋마다 갱신. 순서: MCP·스킬(`c04d42f`) → 웰컴/정보/팔레트(`f5512a1`) → 카탈로그/가져오기(`ef12efa`) → 시스템 모니터·디버그·메뉴바·단축어(`44a13c7`) → 창 제목/앱 메뉴/마크다운(`c3102d9`). 함정: `import`는 예약어라 `L10n.MCP.importButton`, `L10n.MenuBar` 중복 선언 → 메뉴바 팝오버 키는 `L10n.StatusMenu`, `PaletteViews` 401줄 → 서버 제목을 지역 `let`으로 접어 400줄 유지, 차트 `series:`/`chartForegroundStyleScale` 식별자는 범례가 숨겨져 표시 문자열이 아니므로 한국어 유지.
13. T-364 카탈로그 확정(2커밋): `LocalizationKeysCore.swift` 신규(Config·ChatError·Perf·ToolArg·Downloader·EngineNotice·Power)로 표시 문자열이 남아 있던 Core 로직까지 전환 — `ConfigStore.diffSummary`, `ModelCatalog` blurb/오류, `ToolCallFormat`, `ChatStore.perfLine`·오류 버블 6종, `NativeEngine` 준비 실패 사유, `ModelDownloader` fail 5종, 세션 기본 제목(`session.new` 재사용), `BatteryStatus.powerLine`, `FollowUpChipsView` 툴팁·접근성. 테스트가 한국어를 단언하는 부분(`perfLine`·`powerLine`·`모델 파일 없음`·`내용/코드 N자`)은 ko 값이 원문과 동일해 그대로 통과. 다음: T-365 korean-humanizer(ko 값만 수정) → T-366 InfoPlist.xcstrings·오류 카탈로그·문서 → `feat/i18n` push·PR(사용자 지시로 보류).
14. T-365 문구 다듬기: 카탈로그 ko·en 814키를 korean-humanizer 패턴으로 전수 스캔(번역체·이중피동·hype·결산 피벗 0건). 연결어미 뒤 쉼표 1건(`skill.noSkillMD`)과 줄표 표시 문구 20건만 정리 — menuBar.tooltip/failed, engineNotice.noModel, import.*, inspector.*, server.*, sidebar.externalRestart/status.help, welcome.*, exa.test.success, settings.followUpEnabled.help, benchmark.estimate.*(en 범위는 하이픈). 런타임 조합 툴팁 2곳도 가운뎃점 통일, 테스트 단언 3건 동기화. 로그·도구 결과·프롬프트·초성 검색은 대상 제외.
15. T-366 시스템 표면·문서: `InfoPlist.xcstrings`(권한 4건 ko/en) + pbxproj 등록, `InfoPlist.strings` 컴파일 검증 테스트(+1=319). `error_message_ko.json`(29개)을 `error.<코드>` 키로 이관(사용자 선택: 코드별 키 추가 후 JSON 삭제) — `Core/ErrorCatalog.swift`(codes·message) 추가, JSON·pbxproj·테스트 번들 리소스 제거, JSON 로드 테스트를 코드 전수 ko/en 해석 테스트로 교체. pbxproj 함정: 소스 페이즈 3000…001=앱, 3000…004=테스트 — 처음에 테스트 타깃에 잘못 넣어 빌드 실패(L 심볼 미탐색). README(한/영)·사용설명서·CHANGELOG·TODO·PLAN 갱신. 다음: 창/메뉴 제목 즉시 갱신 눈확인(다음 실행 반영), 그 뒤 `feat/i18n` push·PR.
16. 마무리: `feat/i18n`(22커밋, main 대비 84파일 +16,208/−933) push 후 PR #3 생성 https://github.com/BoraSarang/LiteRT-LM-Studio/pull/3 (base main). 검증 319/319·lint 0. 남은 눈확인: 언어 전환 후 창 제목·앱 메뉴가 다음 실행에 반영되는지, 시스템 권한 대화상자 문구.
17. 릴리스: PR #3 main 머지(`fb574be`) → 태그 `v0.7.150` → GitHub Release 생성(https://github.com/BoraSarang/LiteRT-LM-Studio/releases/tag/v0.7.150). README 한/영에 스크린샷 4종(docs/images, chat/settings-language/model-catalog/benchmark) 추가, MARKETING_VERSION 0.7.150·CURRENT_PROJECT_VERSION 136. CHANGELOG 정리: Unreleased에 쌓여 있던 136개 상세 항목(T-0006~T-366, 409줄)을 `docs/CHANGELOG-DETAIL.md`로 분리하고 CHANGELOG.md는 릴리스 요약(3~5줄)만 두는 규칙을 머리말에 명시. 머지 후 main 재빌드·설치·실행 완료(0.7.150).
18. 정리 마무리: PR #4("CHANGELOG 요약/상세 분리") 머지(`f581adb`), 0.7.150 릴리스 이후 상태 정리 — 로컬 브랜치 8개(전부 main에 머지: chore/macos-commit-* 4, docs/readme-license, feat/macos-* 3)와 원격 브랜치(feat/i18n·docs/changelog-cleanup·docs/readme-license) 삭제 → **원격에는 `main`만** 남김. TODO에 T-367(릴리스 기록)·T-368(남은 눈확인) 추가, PLAN에 릴리스 절 추가.
19. 앱 자체 업데이트 확인+릴리스 0.7.152: 가이드(`development/guide/macos-app-update.md`, Etchost 검증) 적용 검토 → 기존 `ReleaseNotes` 인프라 재사용. `newerApp`(앱 출처 판정)·`UpdateCheckFrequency`(실행 시/매일/매주/안 함)+`shouldCheck` 순수함수·`appUpdate()`·`autoCheckAppUpdate()`(UserDefaults 영속). 버그 수정: `merge()` 합산 cap 20에 앱이 잘려 "릴리스 없음"이 뜨던 문제 → 출처별 cap(엔진 20·앱 10) 분리. `featured` 게시일순 혼합 3건+Studio/litert-lm 뱃지. UI: 앱 메뉴 업데이트 확인(정보 창 열고 자동 확인)·정보 창 확인 행+시트(체인지로그 스크롤+DMG 안내+릴리스 페이지)+팝오버 종료 왼쪽 버전/업데이트+빈 화면 envLine Studio+사이드바 라벨 Studio+설정 확인 주기+시작 시 자동 확인. `update.*` 15키(ko/en)+인덱스. 커맨드 팔레트 빈 질의 개편(최근+나머지 전체+지우기, fallback 제거). 가이드 ZIP 표현 DMG 정리. 검증: 신규 4건 포함 전체 12스위트 통과·lint 0. 버전 0.7.150으로 낮춰 실기 검증 후 0.7.151 복원. 커밋 `40e1646`→main 직접 push(사용자 지시)→태그 `v0.7.152`→CI 성공→Release 발행(DMG+sha256). MARKETING 0.7.152·build 138. README(한/영)·CHANGELOG 갱신. 설치·실행 확인.

## 마무리 요약

* 완료: T-361~T-366(한/영 다국어 전환·문구 다듬기·시스템 표면) + 릴리스 0.7.150 + 문서 정리.
* 검증: 단위 테스트 319/319, swiftlint 0(벤더 Sendable 경고 2건은 T-010 정책상 유지), 앱 0.7.150 설치·실행 확인.
* 산출물: PR #1~#4 머지, 태그 `v0.7.150`, GitHub Release, README(한/영)+스크린샷, docs/CHANGELOG-DETAIL.md.
* 남은 일: T-368 눈확인(창 제목·앱 메뉴 다음 실행 반영, 권한 대화상자 문구) — 사용자 확인 필요.
* 규칙 준수: main 직접 push 없이 전부 PR 경유, 커밋/문서 한국어, 시크릿 하드코딩 없음.
