# session-2026-09-16-macos — PLAN_v30~v40 빈 방·출렁 추적 (T-200~T-214)

1. 무엇을: 라이브 코드 invisibility + 간헐 빈 방. 코드(T-201 확정) → 빈 방 14연속 조치.
   T-200 계측 → T-201 렌더 분리 → T-202 스크롤 단일화 → T-203 검증 →
   T-204 붕괴 보정 → T-205 기준점 갱신 → T-206 앵커 재수렴 → T-207 교체 감지+자 진단 →
   T-208 스윕+7초 확정 → T-209 프록시 우선 착지 → T-210 calm 40 →
   T-211 스윕 삭제+예산 2회 → T-212 동결 깨움+핀 정직화 → T-213 초기보고+진입게이트 →
   T-214 최종 슬롯 예산 우회. 0.7.136~0.7.149.
2. 플랫폼: macOS 단일, SwiftUI만, xcodebuild만. AppKit은 점프 진입점 범위 내.
3. 빌드+PERF+CACHE: full 112/112, lint 기존 5건 동일·신규 0 (워크트리 대조),
   `xcodebuild build` + `build_and_run.sh build` 설치·실행 매번 확인.
   PERF/CACHE 영향 없음.
4. 남은TODO: T-215 잔여 간헐 미도달 방 (재현 조건 특정 후 처방). 핵심 단서:
   진입 시작 문서=이전 방 잔재, 동일 방 높이 널뜀(14183→9611), 앵커·AppKit 자 불일치,
   리사이즈·⌥⌘I로 표시됨(동결), 수동 탈출=화살표 버튼.
5. 전달로그: 스크린샷 직접 확인 1회(완전 백지+화살표). `git stash` 무의미 시 worktree 대조.
   swiftlint 절대경로 호출 시 벤더 제외 미적용 → 상대경로로. frame 인자 순서 1건 수정.
   T-201 `.id` 중복 편집 1건 원복. FollowGate.swift 잘못 자른 시그니처 1건 복원.
6. 문서갱신: PLAN_v30~v40 + TODO T-200~215 + CHANGELOG 0.7.136~149 + DESIGN 스크롤 3줄.
   사용설명서·error_message 변경 없음 (신규 에러코드 없음).
7. 큐상태: main 커밋 15건 (feat/fix/docs), 원격 없음으로 push 생략. 트리 깨끗.
8. E2E: 미해당. full은 매 T마다 실행済み.
