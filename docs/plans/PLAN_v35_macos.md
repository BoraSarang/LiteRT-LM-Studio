# PLAN v35 — 프록시 우선 착지 (macOS)

## 1. 목표

레이아웃 동결 해소. 리사이즈·인스펙터 토글 시 내용 나타남으로 확정.
AppKit 직접 점프는 동결된 추정치를 못 깨고 허공에 남음. T-209 1건.

## 2. 근거

* 0.7.143 로그: 전 보정 발동(위고착·재수렴) + 백지 지속 30초.
* 수동 레이아웃 무효화(리사이즈·⌥⌘I)로 즉시 표시.
* 단 수동 해제 후 하단 자동 이동은 없음 → 해제+착지를 한 묶음으로.

## 3. 변경 (T-209)

* `settleToBottom`: 절대 점프 → 프록시 chatBottom (레이아웃 깨움) →
  0.2초 후 절대 점프 확정+기준 갱신.
* 종료 분기(finish/상한)의 `jumpToBottom`을 `settleToBottom`으로 교체.
  T-167 오버슛은 확정 점프가 실측 기준으로 교정 (지연 보정 6종 유지).
* 버전 0.7.144.

## 4. 검증

* full 112/112 + lint 신규 0 + build 설치. 눈확인: 난감하네↔오른쪽에 왕복,
  수동 리사이즈 없이 하단 표시.
* PERF/CACHE 영향 없음 (진입 종료 시 1회).

## 5. 위험

* 프록시 오버슛 재발: 확정 점프+지연 보정으로 흡수. 과민 시 프록시 단계 제거.
