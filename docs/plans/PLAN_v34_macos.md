# PLAN v34 — 스윕 복구+최종 확정 (macOS)

## 1. 목표

얼어붙은 추정치 깨기. 점프는 추정치 공간 안에서만 움직여 허공 고착을 못 벗어남.
실물 확증: 타임라인 완전 백지+화살표 보임(앵커 아래 존재). T-208 1건.

## 2. 근거

* 수렴·위고착·재수렴이 발동해도 백지 지속 (0.7.142 로그).
* 프록시 재착지는 발동 기록 없음 → 스윕으로 교체 (삭제).
* 스윕(맨 위 앵커→0.4초→하단 절대점프)이 실측을 강제, 7초 최종 확정으로 착지.

## 3. 변경 (T-208)

* 지연 works를 [1.5, 3.0, 5.0, 7.0]으로 확장.
* 5초: `sweepBottomRecover` (고착·초과 판정 + 미이동 가드, 첫 메시지 top →
  0.4초 후 절대 점프+기준 갱신). 정상 방은 가드 스킵이라 플래시 없음.
* 7초: `finalVerifyJump` (고착 가드 절대 점프+기준 갱신).
* `reseatBottomViaProxy` 삭제 (미발동, 스윕으로 대체).
* 버전 0.7.143.

## 4. 검증

* full 112/112 + lint 신규 0 + build 설치. 눈확인: 난감하네↔오른쪽에 왕복.
* PERF/CACHE 영향 없음.

## 5. 위험

* 스윕 플래시 (위→아래 1회): 고착 방에서만, 빈 화면 대신 내용. 허용.
