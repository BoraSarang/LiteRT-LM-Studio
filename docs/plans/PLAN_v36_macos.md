# PLAN v36 — 재수렴 calm 임계 (macOS)

## 1. 목표

정착 후 하단 출렁 제거. 매 진입 뒤끝에 `앵커 재수렴`이 발동해 미세 점프 반복.
T-210 1건 (소규모).

## 2. 근거

* 0.7.144 로그: 전 진입 뒤 1.5초 재수렴 발동, 화면은 표시됨.
* 허용 오차 8pt라 하이라이트 확정·표 늦은 레이아웃 미세 변동에도 점프.
* 진짜 허공은 수백~수천 pt, 잔물결은 수십 pt → 40pt로 구분.

## 3. 변경 (T-210)

* `recoverPastTrueEnd` 호출부 임계 8→40 (순수 함수 기본값 불변).
* 회귀 2건 (39 스킵·41 발동). 버전 0.7.145.

## 4. 검증

* full 112/112 + lint 신규 0 + build 설치. 눈확인: 하단 정착 후 무진동.
* PERF/CACHE 영향 없음.
