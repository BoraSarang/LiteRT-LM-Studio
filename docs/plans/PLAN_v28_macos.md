# PLAN v28 — TTFT 단계 계측 + 처방 (macOS)

## 1. 목표

채팅 TTFT 17~44초의 구간 특정 (bench 0.9초와 차이). 분해 현황:
제한 없음 44~50초 → 10턴 27~35초 → CPU 인코더 17~23초. 잔여 고정 약 20초 미해명.

## 2. 변경

* T-190 계측 (로그만, 동작 불변): `runNative`에 prepare 완료 시점+히스토리
  (개수·문자 수) 1줄 추가. 기존 첫 토큰 TTFT와 조합해
  prepare cost vs (대화 생성+프리필) cost 분리.
* T-191 처방 (계측 결과 후): 프리필 우세면 대화 유지형 전환(KV 이어쓰기),
  고정 cost 우세면 해당 구간 수정.

## 3. 검증

* unit 101 유지 + lint 신규 0 + `build macos` 설치.
* 사용자 전송 1회 → 로그 3종(준비 완료·히스토리·TTFT)으로 판정.
