# PLAN_v106 — 잔여 UI 수정 (T-335, macOS)

> 로직 동결 (UI 텍스트·레이아웃·툴팁만).

## 변경

* 표 셀 svg 링크 (T-331 후속): `[<svg>](url)` → `[아이콘](url)`.
  `styled()` 진입점에서 일괄 (문단·표셀·见出し 공통, 코드스팬 보호).
  원인: 파이프 표 셀 안 svg는 줄 단위 제거 대상이 아니었음 (실물 README 대조).
* 벤치 하단 패딩 12 (맨 아래 잘림).
* `powerLine`: `MTP 켬` → `MTP 제한`, ⓘ `배터리 충전 중에는 속도 제한`.
* HF 토큰: `CatalogEntry.gated` 파싱(false/auto/manual) 후 게이트 모델에만 표시.
* "종지" 0건 스킵 (T-325 확정).

## 검증

* 신규 unit 4건 + 286 회귀, lint 신규 0, build, 눈확인.
