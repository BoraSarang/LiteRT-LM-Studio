# PLAN v40 — 최종 슬롯 예산 우회 (macOS)

## 1. 목표

예산 고갈 좌초 해소. 1.5/3초에 예산 소진 → 7초 최종 recover가 무음 스킵,
핀ON이라 다른 길도 막혀 영구 정지 (찔끔 이동 후 멈춤). T-214 1건.

## 2. 근거 (0.7.148 로그 11:07:55~11:08:03)

* 2개 방: 위고착 2회(예산 소진) → 상한 종료 → 7초 자 불일치(문서 2448·실측 1412)만,
  recover 무음 스킵. 버튼 없음(핀ON).
* 진동은 반복에서 오므로, 반복 없는 최종 1회는 안전.

## 3. 변경 (T-214)

* `FollowGate.finalPass`: 최종 works에서만 true. `claimCorrectionBudget`은
  finalPass면 무조건 통과. 발동 후 false로 복귀 (다음 진입 혼선 방지).
* 버전 0.7.149.

## 4. 검증

* full 112/112 + lint 신규 0 + build 설치. 눈확인: 2개 방 하단 도달+버튼.
* PERF/CACHE 영향 없음.

## 5. 위험

* 최종 슬롯 다중 발동: 각 함수 가드가 점프 후 상태를 재평가라 실제 이동은 1~2회.
  이후 works 없음이라 진동 불가.
